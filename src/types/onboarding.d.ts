
// This file should only contain type declarations that extend existing types
// The main type definitions should be in onboarding.ts
import { SoundPreference as MainSoundPreference } from './onboarding';

// Re-export the type to ensure compatibility with any imports
export type SoundPreference = MainSoundPreference;
